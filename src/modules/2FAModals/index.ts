import enable2FA from "./Enable2FA";
import disable2FA from "./Disable2FA";
import info2FA from "./Info2FA";
import enter2FACode from "./Enter2FACode";

export {
  enable2FA, disable2FA, info2FA, enter2FACode,
};

export default {
  enable2FA, disable2FA, info2FA, enter2FACode,
};
